(define (problem BLOCKS)
  (:domain BLOCKS)
(:goal (AND 
        ; P O R T A L
        (ONTABLE L)
        (ON A L)
        (ON T A)
        (ON R T)
        (ON O R)
        (ON P O)
        )   
    )
)
